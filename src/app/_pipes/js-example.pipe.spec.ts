import { JsExamplePipe } from './js-example.pipe';

describe('JsExamplePipe', () => {

  const pipe = new JsExamplePipe();

  it('create an instance', () => {
    expect(pipe).toBeTruthy();
  });

});
